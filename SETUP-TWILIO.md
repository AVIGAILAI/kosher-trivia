# חיבור מספר טלפון אמיתי (Twilio) — מדריך

הקוד בצד השרת כבר מוכן ונבדק. כדי לחייג ממש מטלפון, צריך לחבר מספר אמיתי.
זה החלק שרק **את** יכולה לעשות (אסור לי לפתוח חשבונות או להזין סיסמאות במקומך).

---

## מה צריך — 4 שלבים

### 1. לפתוח חשבון Twilio (חינם)
- להיכנס ל‑https://www.twilio.com/try-twilio ולהירשם.
- יש **ניסיון חינם** עם קרדיט התחלתי — מספיק בהחלט כדי לבדוק.
- בהרשמה תתבקשי לאמת את **מספר הטלפון שלך** — עשי זאת (כך תוכלי לחייג בעצמך לבדיקה).

### 2. לקבל מספר טלפון
- בקונסולה: **Phone Numbers → Buy a number** (או "Get a trial number").
- לוודא שהמספר תומך ב‑**Voice** (שיחות קול).

### 3. לחבר את המספר לשרת שלנו
כדי ש‑Twilio יוכל להגיע לשרת שרץ אצלך במחשב, נפעיל "מנהרה" (tunnel)
שנותנת כתובת אינטרנט זמנית. **אני אפעיל אותה ואתן לך את הכתובת המדויקת.**

בקונסולה של Twilio, במסך המספר שקנית, תחת **Voice Configuration**:
- **A call comes in** → סוג: `Webhook`
- כתובת: `https://<הכתובת-שאתן-לך>/voice/incoming`
- שיטה: `HTTP POST`
- (רשות) **Call status changes** → `https://<הכתובת>/voice/status` (POST) — כדי שמתקשר שמנתק יוסר מהמשחק.
- לשמור (**Save**).

### 4. לבדוק
- פותחים את **מסך המנחה** (http://localhost:3000) — מקבלים קוד PIN.
- מחייגים מהטלפון למספר Twilio.
- מקישים את קוד ה‑PIN → אמורים לשמוע "התחברת בהצלחה".
- המנחה לוחץ "התחל משחק" → שומעים את השאלה בטלפון ומקישים 1–4.

---

## הערות חשובות
- **הקול בעברית** מגיע מ‑Amazon Polly (קול "Tomer"). אם הקול לא נשמע טוב,
  אפשר לכוונן דרך משתנה סביבה `TWILIO_VOICE` (למשל קול אחר של Polly).
- **חשבון ניסיון**: בתחילת כל שיחה יושמע משפט של Twilio ("trial account") — זה נורמלי.
- **מספר אמריקאי**: מספר ניסיון הוא בד"כ אמריקאי. לבדיקה זה בסדר. מספר ישראלי
  לפלאפונים כשרים אמיתיים — שלב רכש נפרד בהמשך (דורש חשבון בתשלום וכתובת בישראל).
- **המנהרה** חושפת את השרת המקומי לאינטרנט באופן זמני, לצורך הבדיקה בלבד.

---

## משתני סביבה (רשות)
```
TWILIO_VOICE=Polly.Tomer     # קול ההקראה
TWILIO_LANG=he-IL            # שפת ההקראה
```
